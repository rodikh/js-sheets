/**
 * Created by rodik on 13/02/2017.
 */
const chai = require('chai');
const expect = chai.expect;

const JSONSheet = require('../json-sheets');

describe('Querying', function () {
    const arr = [{a: 1, b: 4}, {a: 2, b: 3, c: 6}, {a: 3, b: 4}];
    describe('Selecting', function () {
        xit('should select by property value and operator', function () {});
    });

    describe('reducing and mapping', function () {
        xit('should calculate sum/avg');
    });
});